export default function Pie() {
  return (
    <footer
      style={{
        padding: "10px 20px",
        borderTop: "1px solid var(--border-color)",
        background: "var(--panel-bg)",
        fontSize: "0.9rem",
        opacity: 0.9,
      }}
    >
      Informacion: este panel muestra metricas en tiempo real desde Metabase.
    </footer>
  );
}
